# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
**/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# Frontend specific files (moved to frontend/)
# See frontend/.gitignore for frontend-specific ignores

# Backend specific files
backend/dist/
backend/node_modules/
backend/.env*

# Production builds
/build
**/build
**/dist

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Env files (can opt-in for committing if needed)
.env*
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.development
.env.production

# Vercel
.vercel

# TypeScript
*.tsbuildinfo

# package-lock files (since we're using Yarn)
**/package-lock.json

# Storybook build outputs
**/storybook-static

# cursor params
.cursorrules
.cursorignore
/.cursorrules.backup

# coinlympia specific
sitemap-0.xml
/generated/prisma
